A burger logger with MySQL, Node, Express, Handlebars and a homemade ORM. 
This application follows the MVC design pattern and uses Node and MySQL to query and route data in the app
and uses Handlebars to generate your HTML.